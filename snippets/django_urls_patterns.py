<!--
    category: Django_Urls
    name: add urlpatterns
    toolTip: add urlpatterns to urls.py
    prompt(module): write the module to shortten view name
-->
from django.conf.urls.defaults import *

urlpatterns = patterns('${module}',

)